Find the minimum value iun an array:

public class Main{
    public static void main(String []args){
        int []arr = {3,2,-5,21,10};
        int MinVal = arr[0];
        int n = arr.length;
        for(int i=0 ;i<=n-1; i++){
            if(arr[i]<MinVal){
                
                MinVal = arr[i];
             }
        }
        System.out.println("MIN value is " +MinVal);
    }
}
